name: Verify Versions
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  verify-versions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Verify committed versions are in sync
        run: ./scripts/verify-versions.sh
